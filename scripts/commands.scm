; This file is part of Embroidermodder 2.
;
; Loading all commands used by the Embroidermodder interface.

; (load "scripts/circle.scm")

; Aliases
;
(define pandown pan-down-f)
(define panleft pan-left-f)
(define panright pan-right-f)
(define panup pan-up-f)

(define zoomextents zoom-extents-f)
(define zoomin zoom-in-f)
(define zoomout zoom-out-f)
