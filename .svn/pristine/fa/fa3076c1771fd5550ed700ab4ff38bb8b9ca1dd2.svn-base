// Messaging.h
//
// This sourcecode file is a part of embroidermodder.
// This program is free software; you can use, redistribute, and/or modify it
// under the terms of the GNU General Puplic License (GPL) as published by
// the Free Software Foundation.  See the file license.txt for details.
// http://embroidermodder.sourceforge.net/
//

#if !defined(MESSAGING_H__INCLUDED_)
#define MESSAGING_H__INCLUDED_

class status_io {
public:
	CStatusBarCtrl  *StatusBar;

	void statusbar(void);	// constructor
	void statusbar_settext(const char *src,int field=0);
	void set(char *src);
	void set(int src);
	void set(double src);
	void set(const char *src1, const char *src2);
	void set(const char *src1, int src2);
	void set(const char *src1, double src2);
	void set_size(class pattern *pattern);
	void set_scale(class pattern *pattern);
	void set_colors(class pattern *pattern);
	void set_format(class pattern *pattern);
	void clear(void);
	void updateall(class pattern *pattern);
	void clearall(void);

	/*
	void statusbar_settext(const char *src,int field=0);
	void status(char *src);
	void status(int src);
	void status(double src);
	void status(const char *src1, const char *src2);
	void status(const char *src1, int src2);
	void status(const char *src1, double src2);
	void status_size(class pattern *pattern);
	void status_scale(class pattern *pattern);
	void status_colors(class pattern *pattern);
	void status_format(class pattern *pattern);
	void status_clear(void);
	void status_updateall(class pattern *pattern);
	void status_clearall(void);
	*/
 };

class messages {
public:
	CHAR *text_buffer;
	unsigned int text_buffer_allocated;
public:
	messages();		// constructor
	~messages();	//destructor
	void add(const char *src);
	void add(int src);
	void add(double src);
	// The following have an automatic newline added...
	void add(const char *src1, const char *src2);
	void add(const char *src1, int src2);
	void add(const char *src1, double src2);
	void clear(void);
};

#include "embroidermodder.h"

#endif