Stuff that is now supposed to be generated by Doxygen\+:

\begin{DoxyRefDesc}{Todo}
\item[\mbox{\hyperlink{todo__todo000189}{Todo}}]Bibliography style to plainnat.\end{DoxyRefDesc}


\begin{DoxyRefDesc}{Todo}
\item[\mbox{\hyperlink{todo__todo000190}{Todo}}]Serif font for printed docs.\end{DoxyRefDesc}


\begin{DoxyRefDesc}{Todo}
\item[\mbox{\hyperlink{todo__todo000191}{Todo}}]US letter paper version of printed docs. /$\ast$ TODO\+: Josh, Review this file and move any info still valid or needing work into TODO comments in the actual
\begin{DoxyItemize}
\item libembroidery code. Many items in this list are out of date and do not reflect the current status of
\item libembroidery. When finished, delete this file. $\ast$/
\end{DoxyItemize}\end{DoxyRefDesc}



\begin{DoxyItemize}
\item Test that all formats read data in correct scale (format details should match other programs)
\item Add which formats to work with to preferences.
\item Check for memory leaks
\item Update all formats without color to check for edr or rgb files
\item Fix issues with DST (VERY important that DST work well)
\end{DoxyItemize}

\DoxyHorRuler{0}
 \hypertarget{_ideas_autotoc_md13}{}\doxysubsubsection{libembroidery C formats}\label{_ideas_autotoc_md13}
\hypertarget{_ideas_autotoc_md14}{}\doxysubsubsection{FORMAT $\vert$ READ  $\vert$ WRITE $\vert$ NOTES}\label{_ideas_autotoc_md14}
10o $\vert$ YES $\vert$ $\vert$ read (need to fix external color loading) (maybe find out what ctrl code flags of 0x10, 0x08, 0x04, and 0x02 mean) 100 $\vert$ $\vert$ $\vert$ none (4 byte codes) 61 00 10 09 (type, type2, x, y ?) x \& y (signed char) art $\vert$ $\vert$ $\vert$ none bro $\vert$ YES $\vert$ $\vert$ read (complete)(maybe figure out detail of header) cnd $\vert$ $\vert$ $\vert$ none col $\vert$ $\vert$ $\vert$ (color file no design) read(final) write(final) csd $\vert$ YES $\vert$ $\vert$ read (complete) dat $\vert$ $\vert$ $\vert$ read () dem $\vert$ $\vert$ $\vert$ none (looks like just encrypted cnd) dsb $\vert$ YES $\vert$ $\vert$ read (unknown how well) (stitch data looks same as 10o) dst $\vert$ YES $\vert$ $\vert$ read (complete) / write(unknown) dsz $\vert$ YES $\vert$ $\vert$ read (unknown) dxf $\vert$ $\vert$ $\vert$ read (Port to C. needs refactored) edr $\vert$ $\vert$ $\vert$ read (C version is broken) / write (complete) emd $\vert$ $\vert$ $\vert$ read (unknown) exp $\vert$ YES $\vert$ $\vert$ read (unknown) / write(unknown) exy $\vert$ YES $\vert$ $\vert$ read (need to fix external color loading) fxy $\vert$ YES $\vert$ $\vert$ read (need to fix external color loading) gnc $\vert$ $\vert$ $\vert$ none gt $\vert$ $\vert$ $\vert$ read (need to fix external color loading) hus $\vert$ YES $\vert$ $\vert$ read (unknown) / write (C version is broken) inb $\vert$ YES $\vert$ $\vert$ read (buggy?) jef $\vert$ YES $\vert$ $\vert$ write (need to fix the offsets when it is moving to another spot) ksm $\vert$ YES $\vert$ $\vert$ read (unknown) / write (unknown) pcd $\vert$ $\vert$ $\vert$ pcm $\vert$ $\vert$ $\vert$ pcq $\vert$ $\vert$ $\vert$ read (Port to C) pcs $\vert$ BUGGY $\vert$ $\vert$ read (buggy / colors are not correct / after reading, writing any other format is messed up) pec $\vert$ $\vert$ $\vert$ read / write (without embedded images, sometimes overlooks some stitches leaving a gap) pel $\vert$ $\vert$ $\vert$ none pem $\vert$ $\vert$ $\vert$ none pes $\vert$ YES $\vert$ $\vert$ phb $\vert$ $\vert$ $\vert$ phc $\vert$ $\vert$ $\vert$ rgb $\vert$ $\vert$ $\vert$ sew $\vert$ YES $\vert$ $\vert$ shv $\vert$ $\vert$ $\vert$ read (C version is broken) sst $\vert$ $\vert$ $\vert$ none svg $\vert$ $\vert$ YES $\vert$ tap $\vert$ YES $\vert$ $\vert$ read (unknown) u01 $\vert$ $\vert$ $\vert$ vip $\vert$ YES $\vert$ $\vert$ vp3 $\vert$ YES $\vert$ $\vert$ xxx $\vert$ YES $\vert$ $\vert$ zsk $\vert$ $\vert$ $\vert$ read (complete)

Support for Singer FHE, CHE (Compucon) formats? 